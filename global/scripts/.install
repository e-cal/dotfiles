#!/usr/bin/env bash

cd $HOME/.dotfiles

echo "Generating `hostname` dotfiles..."
stow --dir="$HOME/.dotfiles" --target=`hostname` global --dotfiles -R

echo "Linking `hostname` dotfiles..."
stow `hostname` -R




