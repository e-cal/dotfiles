#!/usr/bin/env bash

launch-bar &
dropbox &
redshift &
eyes &
yay -Syyy &
config-wacom m &

cloud backup &
cloud download all > ~/.local/share/rclone/startup-sync.log &
# cloud autosync &

[[ `hostname`="coeus" ]] && sleep 10 && polybar-msg -p "$(pgrep -f "polybar top")" hook volume 1 &>/dev/null
