#!/usr/bin/env bash


if [[ $# = 0 ]]; then
    msg="Update"
else
    for w in $@; do
        msg+="$w "
    done
fi

cd $STOW_DIR
git submodule foreach "git add -A; git commit -m \"$msg\"" 2>/dev/null
git add -A
git commit -m "$msg"
echo git push --recurse-submodules=on-demand


