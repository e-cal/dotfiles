#!/usr/bin/env bash


if [[ $# = 0 ]]; then
    msg="Update"
else
    for w in $@; do
        msg+="$w "
    done
fi

cd $STOW_DIR
git add -A
git commit -m "$msg"
git submodule foreach "git add -A; git commit -m \"$msg\""
git push --recurse-submodules=on-demand


