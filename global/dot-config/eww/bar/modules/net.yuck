(defwidget net []
  (button
    :class "module icon"
    :onclick "${EWW_CMD} open --toggle system-menu"
    :tooltip {net.name}
    :style "color: ${net.color}; margin-left: 1em"
    "${net.icon} "))
