(defwidget battery []
  (button
    :class "module icon"
    :onclick "~/.config/eww/scripts/toggle system-menu"
    :tooltip "${battery.percent}%"
    :style "color: ${battery.color}"
    "${battery.icon} "))
